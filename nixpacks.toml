# nixpacks.toml

[phases.setup]
nixPkgs = ['...', 'pkgs.elixir_1_15']
aptPkgs = ['...', 'gcc', 'make', 'libc6-dev', 'erlang-dev']

[phases.build]
cmds = ['mix compile', 'mix ecto.setup', 'mix assets.deploy']

# [start]
# cmd = './bin/railway start'
